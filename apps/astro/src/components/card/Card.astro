---
const { className } = Astro.props;
const headingId = `card-heading-${crypto.randomUUID()}`;
---

<section class:list={["card", className]} aria-labelledby={headingId}>
  <slot name="image" />
  <div class="content">
    <div id={headingId}>
      <slot name="heading" />
    </div>
    <slot />
    <slot name="call-to-action" />
  </div>
</section>

<style>
  .card {
    background-color: var(--sds-color-layout-background-default);
    border-radius: 24px;
    display: flex;
    flex-direction: column;
    gap: var(--sds-space-padding-medium);
    padding: var(--sds-space-padding-medium);

    img {
      /* TODO: class on slot */
      border-radius: 16px;
    }
  }

  .content {
    display: flex;
    flex-direction: column;
    gap: var(--sds-space-padding-medium);
  }

  .reverse {
    flex-direction: column-reverse;

    @container page (min-width: 760px) {
      flex-direction: row-reverse;

      > * {
        flex: 1;
      }
    }
  }

  .large {
    --padding: var(--sds-space-padding-large);
    padding: var(--padding);

    img {
      margin-bottom: calc(var(--padding) * -1);
    }

    @container page (min-width: 760px) {
      gap: 96px;
    }
  }
</style>
