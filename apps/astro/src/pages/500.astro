---
import Layout from "../layouts/Layout.astro";
import { Hero } from "../components";
import { BackButton } from "../components";

const pageTitle = "En feil har skjedd";
---

<Layout title={pageTitle}>
  <Hero
    breadcrumbs={[{ title: "Designsystem", href: "/" }]}
    heading={pageTitle}
  >
    <BackButton />
  </Hero>
</Layout>
