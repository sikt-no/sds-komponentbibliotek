# Environment settings that apply to all environments
# This file is committed to the repository and should NOT contain any secrets
# Only include configuration values that are safe to share publicly
ASTRO_TELEMETRY_DISABLED=1
PUBLIC_STORYBOOK_URL=/storybook
