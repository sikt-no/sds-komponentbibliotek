.sds-radio {
  --radio-border-color: var(--sds-color-surface-neutral);
  --radio-background-color: var(--sds-color-surface-action);

  align-items: center;
  cursor: pointer;
  display: flex;

  &__input {
    appearance: none;
    background: var(--sds-color-surface-default);
    border: var(--sds-border-size-regular) solid var(--radio-border-color);
    border-radius: var(--sds-border-radius-full);
    display: grid;
    place-content: center;
    pointer-events: none;
    width: var(--sds-size-base-m);
    height: var(--sds-size-base-m);
    padding: 0;
    margin: var(--sds-size-base-s);

    &-label {
      padding: var(--sds-size-base-s) var(--sds-size-base-xs);
      line-height: var(--sds-size-text-m);
    }

    &:checked {
      border-color: transparent;
      background: var(--radio-background-color);

      &::before {
        content: "";
        display: block;
        border-radius: 50%;
        width: var(--sds-size-base-s);
        height: var(--sds-size-base-s);
        background: var(--sds-color-text-onsurface-action);
      }
    }

    &:hover {
      --radio-border-color: var(--sds-color-surface-neutral-hover);
      --radio-background-color: var(--sds-color-surface-action-hover);
    }

    &:active {
      --radio-border-color: var(--sds-color-surface-neutral-active);
      --radio-background-color: var(--sds-color-surface-action-active);
    }

    &:focus-visible {
      --radio-border-color: transparent;

      outline-offset: 0;
      outline: var(--sds-outline);
    }
  }

  &-text {
    font-size: var(--sds-size-text-s1);
    line-height: var(--sds-size-text-m);
    color: var(--sds-color-text-default);
  }

  &--error &__input {
    --radio-border-color: var(--sds-color-surface-danger);
    --radio-background-color: var(--sds-color-surface-danger);

    &:hover {
      --radio-border-color: var(--sds-color-surface-danger-hover);
      --radio-background-color: var(--sds-color-surface-danger-hover);
    }

    &:active {
      --radio-border-color: var(--sds-color-surface-danger-active);
      --radio-background-color: var(--sds-color-surface-danger-active);
    }
  }

  &:has(&__input:disabled) {
    cursor: inherit;
  }
}
