.sds-figure {
  /* TODO: no design provided for element */
  margin: 0;

  &__figure {
    width: 100%;

    &--ratio-16x9 {
      aspect-ratio: 16 / 9;
    }
  }

  &__caption {
    font-size: var(--sds-size-text-s);
    line-height: var(--sds-size-text-s1);
  }
}
