import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as tokens from "../core/dist/tokens/js/tokens.js";

<Meta title="Core/Spacing" />

# Spacing

## Base

<div
  style={{
    display: "grid",
    gridColumnGap: "20px",
    gridTemplateColumns: "120px auto",
  }}
>
  <div
    style={{
      fontSize: "14px",
      fontWeight: "700",
      color: "rgba(51,51,51,0.6)",
      paddingBottom: "20px",
    }}
  >
    Name
  </div>
  <div
    style={{
      fontSize: "14px",
      fontWeight: "700",
      color: "rgba(51,51,51,0.6)",
      paddingBottom: "20px",
    }}
  >
    Size
  </div>
  {Object.entries(tokens.size.base).map((token) => {
    const size = token[1].value;
    const isPrimary = !/\d/.test(token[1].attributes.item);
    return (
      <>
        <div>
          <div
            style={{ fontSize: "14px", fontWeight: "700", color: "#333333" }}
          >
            {token[0]}
          </div>
          <div style={{ color: "rgba(51,51,51,0.8)", fontSize: "14px" }}>
            {token[1].attributes.category}.{token[1].attributes.type}.
            {token[1].attributes.item}
          </div>
        </div>
        <div>
          <div
            style={{
              width: size,
              height: "85px",
              background: isPrimary
                ? tokens.color.base.background.dark.value
                : tokens.color.typography.dark.disabled.value,
            }}
          />
        </div>
      </>
    );
  })}
</div>
