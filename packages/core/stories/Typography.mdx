import { Meta } from "@storybook/blocks";

<Meta title="Core/Typography" />

# Typography

## Editorial Headline

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Example</th>
      <th>Class</th>
    </tr>
  </thead>
  <tbody>
    {["editorial"].map((variant) => {
      return ["xxl", "xl", "l", "m", "s", "xs", "xxs"].map((size) => {
        return (
          <tr key={`${variant}-${size}`}>
            <th scope="row">
              {variant} / headline / {size}
            </th>
            <td className={`sds-typography-${variant}-headline--${size}`}>
              Aa
            </td>
            <td>
              .sds-typography-{variant}-headline--{size}
            </td>
          </tr>
        );
      });
    })}
  </tbody>
</table>

## Application Headline

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Example</th>
      <th>Class</th>
    </tr>
  </thead>
  <tbody>
    {["application"].map((variant) => {
      return ["xxl", "xl", "l", "m", "s", "xs", "xxs"].map((size) => {
        return (
          <tr key={`${variant}-${size}`}>
            <th scope="row">
              {variant} / headline / {size}
            </th>
            <td className={`sds-typography-${variant}-headline--${size}`}>
              Aa
            </td>
            <td>
              .sds-typography-{variant}-headline--{size}
            </td>
          </tr>
        );
      });
    })}
  </tbody>
</table>

## Label

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Example</th>
      <th>Class</th>
    </tr>
  </thead>
  <tbody>
    {["l", "default"].map((item) => {
      const postfix = item !== "default" ? `--${item}` : "";
      return (
        <tr key={item}>
          <th scope="row">label / {item}</th>
          <td className={`sds-typography-label${postfix}`}>Aa</td>
          <td>.sds-typography-label{postfix}</td>
        </tr>
      );
    })}
  </tbody>
</table>

## Overline

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Example</th>
      <th>Class</th>
    </tr>
  </thead>
  <tbody>
    {["default"].map((item) => {
      const postfix = item !== "default" ? `--${item}` : "";
      return (
        <tr key={item}>
          <th scope="row">overline / {item}</th>
          <td className={`sds-typography-overline${postfix}`}>Aa</td>
          <td>.sds-typography-overline{postfix}</td>
        </tr>
      );
    })}
  </tbody>
</table>

## Body

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Example</th>
      <th>Class</th>
    </tr>
  </thead>
  <tbody>
    {["l", "default", "s"].map((item) => {
      const postfix = item !== "default" ? `--${item}` : "";
      return (
        <tr key={item}>
          <th scope="row">body / {item}</th>
          <td className={`sds-typography-body${postfix}`}>Aa</td>
          <td>.sds-typography-body{postfix}</td>
        </tr>
      );
    })}
  </tbody>
</table>

## Other

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Example</th>
      <th>Class</th>
    </tr>
  </thead>
  <tbody>
    {["emphasis", "strong", "quote", "code", "tabular-nums"].map((item) => {
      return (
        <tr key={item}>
          <th scope="row">{item}</th>
          <td className={`sds-typography-body sds-typography--${item}`}>Aa</td>
          <td>.sds-typography--{item}</td>
        </tr>
      );
    })}
  </tbody>
</table>
