import { Meta } from "@storybook/blocks";
import * as tokens from "../dist/js/tokens.js";
import * as tablet from "../dist/js/tokens.tablet.js";
import * as desktop from "../dist/js/tokens.desktop.js";

<Meta title="Tokens/Typography" />

# Typography

## Heading

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>@Mobile</th>
      <th>@Tablet</th>
      <th>@Desktop</th>
      <th>Token</th>
    </tr>
  </thead>

  <tbody>
    {Object.entries(tokens.typography.heading).map((token) => {
          return (
            <tr key={token[1].name}>
              <td>{token[0]}</td>
              <td>{token[1].value}</td>
              <td>{tablet.typography.heading?.[token[0]]?.value ?? ""}</td>
              <td>{desktop.typography.heading?.[token[0]]?.value ?? ""}</td>
              <td>{`--sds-${Object.values(token[1].path).join("-")}`}</td>
            </tr>
          );
        })}
  </tbody>
</table>

## Body

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Value</th>
      <th>Token</th>
    </tr>
  </thead>

  <tbody>
    {Object.entries(tokens.typography.body).map((token) => {
          return (
            <tr key={token[1].name}>
              <td>{token[0]}</td>
              <td>{token[1].value}</td>
              <td>{`--sds-${Object.values(token[1].path).join("-")}`}</td>
            </tr>
          );
        })}
  </tbody>
</table>

## Weight

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Value</th>
      <th>Token</th>
    </tr>
  </thead>

  <tbody>
    {Object.entries(tokens.typography.weight).map((token) => {
          return (
            <tr key={token[1].name}>
              <td>{token[0]}</td>
              <td>{token[1].value}</td>
              <td>{`--sds-${Object.values(token[1].path).join("-")}`}</td>
            </tr>
          );
        })}
  </tbody>
</table>
