import { Meta } from "@storybook/addon-docs/blocks";
import tokens from "../dist/js/tokens.mjs";

<Meta title="Tokens/Layout" />

# Layout

## Base

### Breakpoint

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Value</th>
      <th>Token</th>
    </tr>
  </thead>

  <tbody>
    {Object.entries(tokens.base.breakpoint).map((token) => {
          return (
            <tr key={token[1].name}>
              <td>{token[0]}</td>
              <td>{token[1].$value}</td>
              <td>{`--sds-${Object.values(token[1].path).join("-")}`}</td>
            </tr>
          );
        })}
  </tbody>
</table>

### Elevation

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Value</th>
      <th>Token</th>
    </tr>
  </thead>

  <tbody>
    {Object.entries(tokens.base.zindex).map((token) => {
          return (
            <tr key={token[1].name}>
              <td>{token[0]}</td>
              <td>{token[1].$value}</td>
              <td>{`--sds-${Object.values(token[1].path).join("-")}`}</td>
            </tr>
          );
        })}
  </tbody>
</table>
