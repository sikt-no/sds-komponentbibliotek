# `@sikt/sds-chip`

## Consume

```sh
npm i -s @sikt/sds-chip
```

### React

```js
import { Chip } from "@sikt/sds-chip";
import "@sikt/sds-chip/dist/index.css";

<Chip>My Chip</Chip>;
```

### Stylesheets & custom markup

Import stylesheet:

```css
@import url("@sikt/sds-chip");
```

Create custom markup:

```html
<!-- see html example in Storybook -->
```
