.sds-tab-link {
  display: inline-flex;
  text-decoration: none;

  &--selected {
    border-color: var(--sds-color-interaction-primary-strong-default);
  }

  &:focus-visible {
    outline-offset: 0;
  }
}
