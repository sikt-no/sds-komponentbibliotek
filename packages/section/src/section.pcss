@import url("@sikt/sds-tokens/dist/css/custom-media.css");

.sds-section {
  display: flex;
  flex-direction: column;

  &__header {
    border-bottom: var(--sds-space-border-weight-regular) solid
      var(--sds-color-layout-divider-subtle);
    display: flex;
    flex-direction: column;
    padding-bottom: var(--sds-space-gap-large);
  }

  &__heading {
    padding-bottom: var(--sds-space-gap-medium);
  }

  &__cta {
    display: flex;
    justify-content: flex-end;
  }

  &__content {
    padding-top: var(--sds-space-gap-large);
  }

  @media (--sds-base-breakpoint-tablet) {
    &__header {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }

    &__heading {
      padding-bottom: 0;
    }
  }
}
